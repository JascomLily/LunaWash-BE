using System; using System.Net; using System.Net.Mail; public class Program { public static void Main() { var smtpServer = "smtp.gmail.com"; var port = 587; var senderEmail = "lunawash.noreply@gmail.com"; var password = "kzvwjrjpmjbypdmv"; using (var client = new SmtpClient(smtpServer, port)) { client.UseDefaultCredentials = false; client.Credentials = new NetworkCredential(senderEmail, password); client.EnableSsl = true; var mailMessage = new MailMessage { From = new MailAddress(senderEmail, "LunaWash"), Subject = "Test Email", Body = "Test Body", IsBodyHtml = true }; mailMessage.To.Add(senderEmail); try { client.Send(mailMessage); Console.WriteLine("Email sent successfully"); } catch (Exception ex) { Console.WriteLine("Error sending email: " + ex.Message); } } } }
